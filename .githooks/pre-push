#!/usr/bin/env bash
set -e

cd "$(git rev-parse --show-toplevel)/dashboard-app"

echo "→ check (lint + types)..."
pnpm run check

echo "→ unit tests..."
pnpm vitest run

echo "→ build..."
pnpm run build

echo "✓ local CI passed"
